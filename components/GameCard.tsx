import Image from "next/image"; import Link from "next/link"; import type { Game } from "@/content/games";
export function GameCard({game}: {game:Game}){return <article className="game-card"><Image src={game.heroImage} alt={`${game.title} key art`} fill sizes="(max-width: 800px) 100vw, 80vw" style={{objectPosition:game.heroPosition}}/><div className="game-card-overlay"><div className="eyebrow">{[game.status,game.platform].filter(Boolean).join(" · ")}</div><h3>{game.title}</h3><p>{game.tagline}</p><div className="actions"><Link className="text-link" href={`/games/${game.slug}`}>View game <span>↗</span></Link>{game.robloxUrl&&<a className="text-link" href={game.robloxUrl}>Roblox ↗</a>}</div></div></article>}
