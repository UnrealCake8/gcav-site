"use client";
import Image from "next/image"; import Link from "next/link"; import { useState } from "react"; import type { Game } from "@/content/games"; import { activeSocialLinks } from "@/content/social";
export function HeroCarousel({items}:{items:Game[]}){const [i,setI]=useState(0);let start=0;const game=items[i]; const discord=game.discordUrl||activeSocialLinks.find(x=>x.label==="Discord")?.url; const move=(d:number)=>setI((i+d+items.length)%items.length);return <section className="hero" aria-roledescription="carousel" aria-label="Featured games" tabIndex={0} onKeyDown={e=>{if(e.key==="ArrowLeft")move(-1);if(e.key==="ArrowRight")move(1)}} onTouchStart={e=>start=e.touches[0].clientX} onTouchEnd={e=>{const d=e.changedTouches[0].clientX-start;if(Math.abs(d)>45)move(d<0?1:-1)}}><Image key={game.slug} className="hero-image" src={game.heroImage} fill priority alt={`${game.title} key art`} sizes="100vw" style={{objectPosition:game.heroPosition}}/><div className="hero-wash"/><div className="hero-content"><span className="hero-index">FEATURED / {String(i+1).padStart(2,"0")}</span><p className="eyebrow">{game.title}</p><h1>{game.tagline.replace(" in ","\nin ")}</h1>{game.description&&<p className="lede">{game.description}</p>}<div className="actions"><Link className="button primary" href={`/games/${game.slug}`}>Learn More</Link>{game.robloxUrl&&<a className="button secondary" href={game.robloxUrl}>Play on Roblox</a>}{discord&&<a className="button secondary" href={discord}>Join Discord</a>}</div></div>{items.length>1&&<div className="carousel-controls"><button onClick={()=>move(-1)} aria-label="Previous featured game">←</button><button onClick={()=>move(1)} aria-label="Next featured game">→</button></div>}<span className="scroll-cue" aria-hidden="true">SCROLL <i/></span></section>}
