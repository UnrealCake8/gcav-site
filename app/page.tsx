import Link from "next/link"; import { HeroCarousel } from "@/components/HeroCarousel"; import { GameCard } from "@/components/GameCard"; import { featuredGames,games } from "@/content/games"; import { news } from "@/content/news"; import { site } from "@/content/site"; import { activeSocialLinks } from "@/content/social";
export default function Home(){return <><HeroCarousel items={featuredGames}/><section className="section games-section"><div className="section-head"><div><p className="eyebrow">THE LINEUP</p><h2>Our Games</h2></div><Link className="text-link" href="/games">View all <span>↗</span></Link></div><div className="games-grid">{games.map(g=><GameCard key={g.slug} game={g}/>)}</div></section><section className="section news-preview"><div className="section-head"><div><p className="eyebrow">FROM THE STUDIO</p><h2>Latest from the Studio</h2></div></div>{news.length===0?<div className="empty"><span>NO TRANSMISSIONS YET</span><h3>Studio updates will appear here.</h3><p>Check back for verified news and development notes.</p></div>:null}</section><section className="about section"><div><p className="eyebrow">ABOUT THE STUDIO</p><h2>A home for<br/>new worlds.</h2></div><div className="about-copy"><p>{site.description}</p><Link className="text-link" href="/developer">Meet the studio <span>↗</span></Link></div><div className="signal" aria-hidden="true"><i/><i/><i/><i/><i/></div></section>{activeSocialLinks.length>0&&<section className="community-cta section"><p className="eyebrow">STAY IN THE LOOP</p><h2>Join the Community</h2><div className="actions">{activeSocialLinks.map(x=><a className="button secondary" href={x.url} key={x.label}>{x.label}</a>)}</div></section>}</>}
