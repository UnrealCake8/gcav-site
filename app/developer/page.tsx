import type { Metadata } from "next"; import Link from "next/link"; import { PageHero } from "@/components/PageHero"; import { site } from "@/content/site"; import { games } from "@/content/games";
export const metadata:Metadata={title:"Developer",description:"Studio information and current projects."};
export default function Developer(){return <><PageHero kicker="INDEPENDENT BY DESIGN" title={site.studioName} description={site.description}/><section className="section split"><div><p className="eyebrow">WHAT WE BUILD</p><h2>Game-first.<br/>World-focused.</h2></div><div><p>STUDIO_DESCRIPTION</p><p className="edit-note">This copy is intentionally editable in <code>content/site.ts</code>. Add an approved studio description there.</p></div></section><section className="section"><div className="section-head"><div><p className="eyebrow">CURRENT WORK</p><h2>Projects</h2></div></div>{games.map(g=><Link className="project-row" key={g.slug} href={`/games/${g.slug}`}><span>{g.title}</span><span>{[g.status,g.platform].filter(Boolean).join(" · ")}</span><b>↗</b></Link>)}</section></>}
