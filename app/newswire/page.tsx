import type { Metadata } from "next"; import { PageHero } from "@/components/PageHero"; import { news } from "@/content/news";
export const metadata:Metadata={title:"Newswire",description:"Verified news and development updates from the studio."};
export default function Newswire(){return <><PageHero kicker="TRANSMISSIONS" title="Newswire" description="News, development notes, and announcements—direct from the studio."/><section className="section">{news.length===0?<div className="empty"><span>THE FEED IS QUIET</span><h2>Studio updates will appear here.</h2><p>No posts have been published yet.</p></div>:<div className="news-grid">{news.map(p=><article key={p.slug}><p className="eyebrow">{p.category} · {p.date}</p><h2>{p.title}</h2><p>{p.excerpt}</p></article>)}</div>}</section></>}
